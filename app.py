import streamlit as st

# إعدادات الصفحة
st.set_page_config(page_title="LAI Advanced Learning", page_icon="🎓", layout="wide")

st.title("🚀 نظام LAI للتحليل التعليمي المتقدم")
st.markdown("---")

# دالة لحساب النتائج والتحليل
def analyze_performance(score, total, subject_name, fields, projects):
    st.subheader(f"📊 تقرير مادة {subject_name}")
    st.write(f"درجتكِ: {score} من {total}")
    
    if score >= 4:
        st.success(f"تحليل LAI: متميزة جداً! نقاط قوتك في: {fields}")
        st.info(f"💡 مشاريع مقترحة لكِ: {projects}")
    elif score >= 2:
        st.warning(f"تحليل LAI: مستوى جيد. يفضل التركيز أكثر على مجال: {fields}")
        st.info("🔗 نشاط مقترح: ابحثي عن فيديوهات تعليمية في هذا المجال وطبقي تجربة عملية.")
    else:
        st.error(f"تحليل LAI: تحتاجين لتقوية أساسياتك في {subject_name}.")
        st.info("📚 خطة تطوير: مراجعة الوحدات الأولى من الكتاب المدرسي.")

# --- 1. قسم العلوم ---
with st.expander("🔬 اختبار العلوم (أسئلة متنوعة)"):
    s1 = st.radio("1. (اختياري) ما هو العضو المسؤول عن ضخ الدم؟", ["الرئة", "القلب", "الكبد"])
    s2 = st.text_input("2. (أكملي) العملية التي يصنع بها النبات غذاءه تسمى البناء ____")
    s3 = st.radio("3. (عللي) لماذا تطفو السفينة فوق سطح الماء؟", ["بسبب قوة الدفع", "بسبب وزنها الثقيل", "بسبب شكلها فقط"])
    s4 = st.text_input("4. (المصطلح) كل ما له كتلة ويشغل حيزاً من الفراغ:")
    s5 = st.radio("5. (صح/خطأ) الخلية هي وحدة بناء الكائن الحي.", ["صح", "خطأ"])
    
    if st.button("تحليل العلوم"):
        s_score = 0
        if s1 == "القلب": s_score += 1
        if "الضوئي" in s2: s_score += 1
        if s3 == "بسبب قوة الدفع": s_score += 1
        if "مادة" in s4 or "المادة" in s4: s_score += 1
        if s5 == "صح": s_score += 1
        analyze_performance(s_score, 5, "العلوم", "الأحياء والفيزياء", "عمل نموذج لخلية نباتية أو دارة كهربائية.")

# --- 2. قسم الرياضيات ---
with st.expander("🔢 اختبار الرياضيات"):
    m1 = st.number_input("1. (حساب) ما ناتج 12 × 12؟", step=1)
    m2 = st.radio("2. (اختياري) المثلث الذي فيه زاوية 90 درجة يسمى مثلث ____ الزاوية.", ["منفرج", "قائم", "حاد"])
    m3 = st.text_input("3. (أكملي) العدد الذي لا يقبل القسمة إلا على نفسه والواحد هو عدد ____")
    m4 = st.number_input("4. (مسألة) إذا كان سعر 5 أقلام هو 10 ريالات، فكم سعر القلم الواحد؟", step=1)
    m5 = st.radio("5. (المصطلح) المسافة حول إطار الشكل الخارجي تسمى:", ["المساحة", "المحيط", "الحجم"])
    
    if st.button("تحليل الرياضيات"):
        m_score = 0
        if m1 == 144: m_score += 1
        if m2 == "قائم": m_score += 1
        if "أولي" in m3: m_score += 1
        if m4 == 2: m_score += 1
        if m5 == "المحيط": m_score += 1
        analyze_performance(m_score, 5, "الرياضيات", "الهندسة والحساب الذهني", "برمجة حاسبة بسيطة أو تصميم أشكال هندسية.")

# --- 3. قسم الإسلامية ---
with st.expander("🕌 اختبار التربية الإسلامية"):
    i1 = st.text_input("1. (أكملي) صلاة العيدين حكمها سنة ____")
    i2 = st.radio("2. (اختياري) كم عدد أركان الإيمان؟", ["5", "6", "7"])
    i3 = st.text_input("3. (المصطلح) اسم يجمع كل ما يحبه الله ويرضاه من الأقوال والأعمال:")
    i4 = st.radio("4. (عللي) الحكمة من الصوم هي تحقيق ____", ["التقوى", "الجوع", "النوم"])
    i5 = st.text_input("5. (أكملي) نزل القرآن الكريم على النبي ﷺ في غار ____")

    if st.button("تحليل الإسلامية"):
        i_score = 0
        if "مؤكدة" in i1: i_score += 1
        if i2 == "6": i_score += 1
        if "العبادة" in i3: i_score += 1
        if i4 == "التقوى": i_score += 1
        if "حراء" in i5: i_score += 1
        analyze_performance(i_score, 5, "الإسلامية", "الفقه والسيرة", "كتابة بحث عن أخلاق النبي ﷺ أو تصميم عرض عن أركان الصلاة.")

# --- 4. قسم الإنجليزية ---
with st.expander("🔤 English Test"):
    e1 = st.selectbox("1. (Grammar) She ____ breakfast every morning.", ["eat", "eats", "eating"])
    e2 = st.text_input("2. (Complete) The opposite of 'Happy' is ____")
    e3 = st.selectbox("3. (Vocabulary) A place where you read books is a ____.", ["Market", "Library", "Zoo"])
    e4 = st.text_input("4. (Translate) ما معنى كلمة 'Apple'؟")
    e5 = st.selectbox("5. (Grammar) ____ you like coffee?", ["Do", "Does", "Is"])

    if st.button("Analyze English"):
        e_score = 0
        if e1 == "eats": e_score += 1
        if "sad" in e2.lower(): e_score += 1
        if e3 == "Library": e_score += 1
        if "تفاح" in e4: e_score += 1
        if e5 == "Do": e_score += 1
        analyze_performance(e_score, 5, "English", "Vocabulary & Grammar", "Watch a short English movie and write 5 sentences about it.")
